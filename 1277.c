#include<stdio.h>
int main()
{
    int t,num;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&num);
        char name[num][50],par[500];
        for(i=0;i<num;i++){
            scanf("%s",name[i]);
            getchar();
        }

    }
    return 0;
}
